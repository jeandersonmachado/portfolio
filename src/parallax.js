var scene = document.getElementById("scene");
var sceneBack = document.getElementById("scene_back");
var parallaxInstance = new Parallax(scene, {
    // scalarX: 5,
    // scalarY: 10,
});

var parallaxInstance2 = new Parallax(sceneBack, {
    // scalarX: 5,
    // scalarY: 10,
});